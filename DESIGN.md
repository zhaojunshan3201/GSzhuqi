# 高采三区生产动态分析系统设计规范

本规范参考 Open Design 的 dashboard skill 思路，但不引入 Open Design 运行时。系统定位是局域网生产调度与措施评价工具，设计目标是清晰、稳定、数据密集、长期可用。

## 设计原则

- 专业生产调度优先：信息密度高、层级清楚、少装饰、少营销感。
- 业务颜色固定：日产液蓝色、日产油红色、含水绿色、日产气黄色、掺油紫色、油比橙色。
- 红色只做品牌和重点强调，不大面积铺底。
- 主要页面结构保持一致：页面标题、筛选区、状态提示、指标卡、图表卡、表格。
- 所有按钮、表单、卡片、折叠面板使用统一圆角 8px。
- 图表必须保留真实数据比例；需要看趋势时使用自适应纵轴，而不是截断数据。

## 视觉系统

- 背景：`#f1f5f9` / `slate-100`
- 主文本：`#0f172a` / `slate-900`
- 次文本：`#64748b` / `slate-500`
- 边框：`#e2e8f0` / `slate-200`
- 品牌红：`#D32F2F`
- 日产液：`#2563eb`
- 日产油：`#D32F2F`
- 含水：`#16a34a`
- 日产气：`#facc15`
- 掺油：`#9c27b0`
- 油比：`#f97316`

## 组件规范

- `.app-shell`：全局应用容器。
- `.app-sidebar`：深色左侧导航，当前菜单使用青绿色强调。
- `.app-header`：顶部页面标题、缓存状态、搜索和用户信息。
- `.app-content`：主内容滚动区，统一页面内边距。
- `.app-card`：标准白色容器，轻阴影、细边框、8px 圆角。
- `.app-card-header`：卡片头部，用于标题、标签、操作按钮。
- `.analysis-section`：带品牌红顶边的主要分析区。
- `.chart-card`：图表容器，固定最小高度，避免图表闪动。
- `.field-control`：所有输入框/下拉框/日期控件基础样式。
- `.action-button`：所有按钮基础样式；配合 `action-primary`、`action-danger`、`action-blue`、`action-outline`。
- `.status-pill`：状态、类别、评价标签。
- `.measure-table`：措施表格专用样式，表头 sticky，行 hover。

## 页面规范

- 系统概览：顶部四个 KPI，下面是数据状态条和全区趋势图。
- 区块分析：区块多选区在顶部，图表按 2 列网格展示，默认近 3 年月度平均。
- 单井分析：筛选条件排列成紧凑网格，曲线区与井列表分离。
- 重点监控：指标卡、含水分布、Top 表格、建议卡片保持同一视觉层级。
- 对比分析：阶段 A、阶段 B、计量站多选、开始分析按钮放在同一个筛选面板内。
- 措施跟踪：筛选区、导入导出、新增、表格使用统一组件。
- 措施分析：饼图、A/B/C/D 卡片、折叠面板、自定义分析使用统一折叠与图表卡。

## 图表规范

- 标题居中，14px 加粗，颜色 `#0f172a`。
- Tooltip 使用白底、浅边框、深色文字。
- 坐标轴使用 `slate-400`，网格线使用浅灰虚线。
- 需要标签的旬度/月度平均曲线显示整数或一位小数，避免长小数。
- ECharts 工具栏保留缩放、还原、保存图片、线柱切换。

